name: CI

# on:
#   push:
#     branches:
#       - main

jobs:
  build:
    name: "Build and Push"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 20.19.2
          cache: "npm"
      - run: npm install
      - run: npm run test

      - name: Generate tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.0.0
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws-cn:iam::123456789100:role/my-github-actions-role

      - name: Login to AWS ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # - name: Login into the container registry
      #   uses: docker/login-action@v4
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Build and push
      #   uses: docker/build-push-action@v7
      #   with:
      #     push: true
      #     tags: paulolemosdev/find-a-friend-api:${{ steps.generate_tag.outputs.sha }}, paulolemosdev/find-a-friend-api:latest
      # - name: Build docker image
      #   run: docker build -t paulolemosdev/find-a-friend-api:${{ steps.generate_tag.outputs.sha }} .

      # - name: Push image
      #   run: docker push paulolemosdev/find-a-friend-api:${{ steps.generate_tag.outputs.sha }}
